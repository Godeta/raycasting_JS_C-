This folder contain an example of raycasting made in C++.

In order to execute it you need a C++ compiler, you can compile it with the command :
g++ main.cpp -o main

And then launch the executable ( just type "main" in the cmd).